from pathlib import Path
R=Path.cwd()
assert __import__('subprocess').check_output(['git','rev-parse','HEAD'],text=True).strip() == '2d94748a983f1aaf84aa274eeda2e92f534ed39a'
def edit(path, changes):
 p=R/path; lines=p.read_text().splitlines(keepends=True)
 for a,b,s in sorted(changes,reverse=True): lines[a:b]=s.splitlines(keepends=True)
 p.write_text(''.join(lines))
edit('spaceship-ui/astro.config.mjs', [(19,19,"import englishCitations from './src/lib/remark/english-citations.mjs';\n"),(74,74,'      englishCitations,\n')])
edit('spaceship-ui/scripts/performance-contract.mjs', [(78,79,'  "data-astro-reload={link.href === \'/about\' || link.href.startsWith(\'/en/\') ? true : undefined}"\n')])
edit('spaceship-ui/src/components/Breadcrumbs.svelte', [(3,3,'    homeHref?: string;\n'),(5,6,"  let { items, homeHref = '/' }: Props = $props();\n"),(12,13,'  <a href={homeHref} class="hover:text-primary transition-colors flex items-center shrink-0"> Home </a>\n')])
edit('spaceship-ui/src/components/Comments.svelte', [(5,5,'  let { lang }: { lang?: string } = $props();\n'),(75,76,"    script.setAttribute('data-lang', lang || config.lang);\n")])
edit('spaceship-ui/src/components/Footer.astro', [(2,2,"interface Props { lang?: 'ko' | 'en'; }\nconst { lang = 'ko' } = Astro.props;\n"),(30,30,'  {lang === \'en\' ? <nav lang="en" aria-label="Policies and translation coverage" class="mt-6 flex flex-wrap gap-5 border-t border-border/60 pt-5 text-xs text-muted-foreground">\n    <a href="/en/translations/">Translation coverage</a><a href="/en/rss.xml">English RSS</a>\n    <a href="/policies" data-astro-reload>Editorial and AI-use policy (Korean)</a>\n    <a href="/policies/disclaimer" data-astro-reload>Disclaimer (Korean)</a>\n    <a href="/policies/copyright" data-astro-reload>Copyright and reuse (Korean)</a>\n  </nav> : <>\n'),(37,37,'  </>}\n')])
edit('spaceship-ui/src/components/Header.astro', [(2,2,"import { englishSwitchPath, nativeAlternates } from '@/lib/locales';\nimport { getEnglishPosts } from '@/lib/english';\n"),(17,19,"const { englishPath } = Astro.props;\nconst isKoreanPage = !Astro.url.pathname.startsWith('/en/');\n"),(21,23,"const isEnglish = currentPath.startsWith('/en/');\nconst localePrefix = isEnglish ? '/en' : '';\nconst postPrefix = `${localePrefix}/posts/`;\nconst koreanPath = nativeAlternates(currentPath)['ko-KR'] || '/';\nconst currentPostSlug = currentPath.startsWith(postPrefix)\n  ? decodeURIComponent(currentPath.slice(postPrefix.length).replace(/\/$/, ''))\n"),(29,32,'    ? isEnglish\n      ? getEnglishPosts().then((posts) => posts.find((post) => post.data.slug === currentPostSlug))\n      : getPublishedPosts().then((posts) => posts.find((post) => getPostSlug(post) === currentPostSlug))\n'),(37,38,"const navLinks: NavLink[] = isEnglish ? [\n  { name: 'Research', href: '/en/research/' }, { name: 'Writing', href: '/en/posts/' },\n  { name: 'News', href: '/en/news/' }, { name: 'About', href: '/en/about/' },\n] : [\n"),(51,53,"  if (currentPostIsNews) return href === `${localePrefix}/news` || href === `${localePrefix}/news/`;\n  const target = href.replace(/\/$/, '');\n  return currentPath.replace(/\/$/, '') === target || currentPath.startsWith(`${target}/`);\n"),(68,69,'        href={isEnglish ? "/en/" : "/"}\n'),(82,83,"              data-astro-reload={link.href === '/about' || link.href.startsWith('/en/') ? true : undefined}\n"),(97,97,'              data-locale-choice="en" data-astro-reload\n'),(106,106,'        {isEnglish && <a href={koreanPath} data-locale-choice="ko" data-astro-reload lang="ko" hreflang="ko-KR" class="language-header-switch" aria-label="한국어로 보기">KO</a>}\n        {isKoreanPage && !showEnglishLink && <a href={englishSwitchPath(currentPath)} data-locale-choice="en" data-astro-reload class="language-header-switch" aria-label="English edition and translation availability" title="English edition — check translation availability">EN</a>}\n'),(119,120,"            data-astro-reload={link.href === '/about' || link.href.startsWith('/en/') ? true : undefined}\n")])
edit('spaceship-ui/src/components/SearchDialog.svelte', [(6,6,'    href?: string;\n'),(44,45,"        const prefix = document.documentElement.lang.toLowerCase().startsWith('en') ? '/en' : '';\n        const res = await fetch(`${prefix}/api/search.json`);\n"),(60,60,'  data-search-dialog\n'),(110,111,'              href={post.href || `/posts/${post.id}`}\n')])
edit('spaceship-ui/src/components/post/NewsFigure.astro', [(4,4,"import englishCaptions from '../../../site/news-media-en.json';\n"),(10,10,"  lang?: 'ko' | 'en';\n"),(11,14,"const { media, priority = false, compact = false, thumbnail = false, lang = 'ko' } = Astro.props;\nconst source = catalogue[media];\nif (!source) throw new Error(`Unknown NEWS media: ${media}`);\nconst captions = lang === 'en' ? (englishCaptions as Record<string, Partial<typeof source>>)[media] : undefined;\nif (lang === 'en' && !captions) throw new Error(`Missing English caption: ${media}`);\nconst item = { ...source, ...captions };\n"),(29,30,'  <a class="news-figure__image" href={item.src} target="_blank" rel="noopener noreferrer" aria-label={`${item.alt} — ${lang === "en" ? "Open full image" : "원본 크게 보기"}`}>\n'),(46,47,'      {item.credit} · <a href={item.source} target="_blank" rel="noopener noreferrer">{lang === "en" ? "Source" : "출처"}</a> ·\n')])
edit('spaceship-ui/src/content.config.ts', [(123,124,"const englishPosts = defineCollection({\n  loader: glob({ pattern: ['**/*.{md,mdx}', '!**/_*'], base: './site/content/english' }),\n  schema: z.object({\n    title: z.string(), description: z.string(), slug: z.string().regex(/^[a-z0-9]+(?:-[a-z0-9]+)*$/),\n    pubDate: z.date(), updatedDate: z.date().optional(), draft: z.boolean().default(false),\n    lang: z.literal('en'), category: z.enum(POST_CATEGORIES), subcategory: z.string(),\n    type: z.enum(POST_TYPES), tags: z.array(z.string()), translationKey: z.string(),\n    translatedPosts: z.record(z.string(), z.string()), featured: z.boolean().default(false),\n    researchArea: z.enum(RESEARCH_AREAS).optional(), researchFeatured: z.boolean().default(false),\n    researchOrder: z.number().optional(), showComments: z.boolean().default(true),\n  }),\n});\nexport const collections = { posts, projects, appearances, about, englishPosts };\n")])
edit('spaceship-ui/src/layouts/Layout.astro', [(6,6,"import { EN_DESCRIPTION, nativeAlternates } from '@/lib/locales';\nimport LanguageHint from '@/components/LanguageHint.astro';\n"),(42,43,'  languageAlternates: providedAlternates = {},\n'),(45,45,"const isEnglish = Astro.url.pathname.startsWith('/en/');\nconst knownAlternates = nativeAlternates(Astro.url.pathname);\nconst languageAlternates = Object.keys(knownAlternates).length ? knownAlternates : providedAlternates;\n"),(46,47,'const pageDescription = description || (isEnglish ? EN_DESCRIPTION : SITE.desc);\n'),(48,49,"const canonicalUrl = new URL(languageAlternates[isEnglish ? 'en' : 'ko-KR'] || (isEnglish ? `${Astro.url.pathname.replace(/\/+$/, '')}/` : Astro.url.pathname), Astro.site);\n"),(57,58,'  getWebSiteSchema(canonicalUrl.toString(), pageLang, pageDescription),\n'),(158,159,'      href={isEnglish ? "/en/rss.xml" : "/rss.xml"}\n'),(205,206,'    <meta property="og:locale" content={isEnglish ? \'en_US\' : pageLang.toLowerCase().startsWith(\'ko\') ? \'ko_KR\' : pageLang.replace(\'-\', \'_\')} />\n'),(250,251,"      {isEnglish ? 'Skip to main content' : '본문으로 건너뛰기'}\n"),(267,267,'        <LanguageHint lang={isEnglish ? "en" : "ko"} />\n'),(270,271,'      <Footer lang={isEnglish ? "en" : "ko"} />\n')])
edit('spaceship-ui/src/lib/experience/post-graph.ts', [(2,2,"type GraphPost = CollectionEntry<'posts'> | CollectionEntry<'englishPosts'>;\n"),(53,54,'function keywords(post: GraphPost) {\n'),(61,62,'export function buildPostGraph(posts: GraphPost[]): PostGraph {\n'),(74,75,"      href: post.collection === 'englishPosts' ? `/en/posts/${post.data.slug}/` : `/posts/${getPostSlug(post)}`,\n")])
edit('spaceship-ui/src/lib/experience/state.ts', [(101,101,"  if (pathname === '/en' || pathname.startsWith('/en/')) pathname = pathname.slice(3) || '/';\n")])
edit('spaceship-ui/src/lib/schemas.ts', [(2,3,'export function getWebSiteSchema(url: string, lang: string = SITE.lang, description: string = SITE.desc) {\n'),(7,8,'    description,\n'),(13,14,'    inLanguage: lang,\n'),(21,21,'  updatedDate?: Date;\n'),(32,33,'    dateModified: (data.updatedDate ?? data.pubDate).toISOString(),\n')])
