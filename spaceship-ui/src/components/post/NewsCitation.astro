---
interface Props { n: number; }
const { n } = Astro.props;
if (!Number.isSafeInteger(n) || n < 1) throw new Error('Invalid NEWS reference number');
---
<a href={`#news-ref-${n}`} data-news-citation={String(n)} aria-label={`참고문헌 ${n}로 이동`} data-astro-reload>[{n}]</a>
