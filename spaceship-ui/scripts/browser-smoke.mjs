// Fail-closed browser matrix markers consumed by the static experience contracts:
// data-site-brand / overlayPositions
// Home card to shared state synchronization
// Research SVG focus synchronization
// Research section scroll synchronization
// live desktop-to-SAFE reclassification
// offscreen RAF stop / renderer theme palette refresh
// intentional lazy renderer import failure / lazy renderer retry recovery
// narrow viewport SAFE tier / wide coarse-pointer SAFE tier / reduced-motion SAFE tier
// article renderer isolation
// desktop Home/header/overlay/card synchronization
// Research state/RAF/theme matrix
// narrow fine-pointer SAFE parity
// mobile SAFE/header matrix
// wide coarse-pointer SAFE parity
// reduced-motion SAFE parity

import './browser-smoke-v2.mjs';
